Hotel 
 -  id                  String       @id @default(uuid())
   userId              String
   categoryId          String
   templateId          String
   
 - main image 
 - services (key-value + image ? )
 - specification (key-value)  
 - about (string + image?) 
 - address 
 - name 